#include "main.h"
/**
* _strpbrk - to get the length of a prefix substring
* @s: pointer s
* @accept: another pointer accept
* Return: returns a pointer to s
*/
char *_strpbrk(char *s, char *accept)
{
	int i;
	char b;

return (s);
}
